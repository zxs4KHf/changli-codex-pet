# v1.1 候选状态与恢复点

更新：2026-07-28（Asia/Shanghai）

## 发布安全边界

- `main`、本机安装版和 `v1.0.0` Release 仍是同一稳定图集，SHA-256 为 `5BC49E16AEA040C5D5101ED4FC8DCB174942079ECD98B18EAE091BBD2B2220FC`。
- v1.1 尚未发布，也没有替换 `%USERPROFILE%\.codex\pets\changli`。
- 本地候选工程：`D:\Codex桌宠\changli-v1.1-candidate`。

## 已完成

1. rows 0–8 重新执行完整结构、联系表与 GIF 检查，0 error、0 warning。
2. failed、waiting、running、review 分别做了动作语义增强候选和按稳定版高度/基线归一化复验。
3. 新标准行虽然动作更明确，但仍有脸型、眼睛大小、线条锐度、饱和度或服装简化差异，因此全部拒绝并回退稳定行。
4. 完成新的 Look 机制文档：`qa/look-mechanics.md`。
5. 完成纯蓝 `#0000FF` 四基准方向。第二版仅使用稳定桌宠素材，`000/090/180/270` 语义通过；生成器输出的 `090/270` 顺序反向，已在提取出的基准槽位层确定性交换，没有镜像、重绘或混合不同生成。
6. 四基准证据：`qa/cardinal-anchors.json`、`qa/cardinal-semantics.json`、`decoded/look-anchors-approved.png`。

## 当前阻塞

- `look-row-9` 必须整条八帧一次生成，不能单格拼补。
- Codex 内置图像通道返回 `model_cooldown`，当时报告的重置时间约 729 小时。
- CLI 备用通道已验证能直连 OpenAI API，但当前本机 `OPENAI_API_KEY` 无效；不要把聊天记录中的旧密钥写入命令、日志或仓库。
- 本机 `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:7892` 会在 Images API TLS 握手阶段超时。若以后使用有效 CLI 凭据，应只在单次子进程内清空代理变量，并先做无密钥、`trust_env=False` 的 401 连通性探测；不要修改系统全局代理。

## 恢复顺序

1. 确认 `qa/review.json` 仍为 0 error、0 warning，`qa/contact-sheet.png` 未变化。
2. 读取 `imagegen-jobs.json`，下一项应为 `look-row-9`。
3. 使用 `prompts/rows/look-row-9.md` 和清单中的五张参考图生成完整 row 9。
4. 复制到 `decoded/look-row-9.png`，立即运行 `assemble_extended_atlas.py` 的 row-9 注册模式，输出 `qa/look-row-9-registered.png` 与 `qa/look-row-9-registration.json`。
5. 在普通桌宠尺寸检查 000→157.5 的八个方向、边缘、身份、低位单马尾、火焰左臂、等量步进和 157.5→180 边界。硬失败必须整行重做。
6. row 9 通过后才生成完整 row 10；它必须同时引用已批准四基准和完成的 row 9。
7. 组装 8×11 后只执行一次 `despill_chroma_edges.py`，再完成 validator、联系表、GIF、16 方向语义、连续性、三票隔离盲测和独立最终视觉 QA。
8. 只有 v1.1 在所有门槛上全面胜过 v1.0.0，才允许替换仓库图集、本机安装版并发布。

## 关键本地证据

- `qa/standard-row-ab-review.md`
- `iterations/standard-rows/rejected-v1/`
- `iterations/standard-rows/targeted-v2/`
- `iterations/standard-rows/normalized-ab/qa/contact-sheet-blue.png`
- `iterations/look/cardinals-v1/`
- `iterations/look/cardinals-v2/`
- `qa/cardinal-semantics.json`
- `qa/look-mechanics.md`

这些候选和迭代文件暂存于 `D:\Codex桌宠`，不属于当前 Release，也不应在未完成最终 QA 前复制到 `pet/changli/`。
